name: bakwaas_fm
description: A simple mockup app
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=2.17.0 <3.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  uni_links: ^0.5.1

flutter:
  uses-material-design: true
